<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.trading.mapper.FeedbackMapper">
    <resultMap id="BaseResultMap" type="com.campus.trading.entity.Feedback">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="content" property="content"/>
        <result column="contact" property="contact"/>
        <result column="rating" property="rating"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO feedbacks (id, user_id, type, content, contact, rating, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{type}, #{content}, #{contact}, #{rating}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM feedbacks ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM feedbacks WHERE id = #{id}
    </select>

    <delete id="delete">
        DELETE FROM feedbacks WHERE id = #{id}
    </delete>
</mapper>
